# Models

Downloaded model weights live here locally and are intentionally ignored by git.

Examples:

```text
models/
  *.gguf
  *.safetensors
  *.onnx
```

Use scripts such as `scripts/download_gemma_gguf.sh` to populate this directory when needed.
