# Configuration

Project-wide configuration files and constants.

## Files
- `vram_budget.yaml` — VRAM allocation budget for each component
- `default_args.yaml` — Default training/inference arguments
